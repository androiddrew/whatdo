# Optional OpenTelemetry dependencies for the `laya-server[otel]` extra (ADR-0002).
# Wired up in the observability ticket (#8); declared here so the extra exists.
# Compile to otel-requirements.txt with `make compile`.
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi
opentelemetry-instrumentation-logging
