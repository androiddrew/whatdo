# Development dependencies (abstract). Compile to dev-requirements.txt with `make compile`.
-r requirements.in

pytest
httpx
ruff
mypy
build

# The official Typesafe SDK: driven by the contract tests to prove the server
# speaks the Jev API. (Contract tests use FakeEngine, so no GPU is needed.)
typesafe-sdk
